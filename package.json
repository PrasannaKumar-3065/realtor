{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev:frontend": "pnpm --filter @workspace/realestate-landing dev",
    "dev:backend": "cd server && NODE_PATH=./node_modules node index.js",
    "dev": "pnpm exec concurrently \"pnpm dev:frontend\" \"pnpm dev:backend\"",
    "start": "PORT=3001 NODE_ENV=production node server/index.js",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:frontend": "pnpm --filter @workspace/realestate-landing build",
    "build:fullstack": "pnpm run build:frontend && cd server && node index.js",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
